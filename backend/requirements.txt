# --- Web framework ---
Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
Flask-Cors==4.0.1
gunicorn==22.0.0

# --- Database (PostgreSQL) ---
# Face vectors are stored as native float8[] arrays and searched with NumPy
# cosine similarity (services/search.py), so no pgvector extension is needed.
psycopg2-binary==2.9.9

# --- Auth / Google OAuth ---
Authlib==1.3.2
PyJWT==2.9.0
Flask-Limiter==3.8.0
google-auth==2.34.0
google-auth-oauthlib==1.2.1
requests==2.32.3

# --- Config ---
python-dotenv==1.0.1

# ===========================================================================
# Face recognition — InsightFace (ArcFace, 512-d) runs in-process
# (services/faces.py). NumPy powers cosine search (services/search.py); Pillow
# resizes uploads + reads EXIF on every upload (services/storage.py). InsightFace
# builds against the NumPy 1.x ABI, so keep numpy pinned < 2.0.
# ===========================================================================
numpy==1.26.4
Pillow==10.4.0
insightface==0.7.3
onnxruntime==1.19.2
opencv-python-headless==4.10.0.84
